[build]
command = "npm install && npm run build"
publish = ".output/public"

[build.environment]
NODE_VERSION = "20"
NETLIFY_USE_PNPM = "false"

# Nuxt will handle routing automatically, no redirects needed
