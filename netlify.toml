[build.environment]
  NODE_VERSION = "18"
  NETLIFY_USE_PNPM = "false"

[build]
  command = "npm install && npm run build"
  publish = ".output/public"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
